const BlogPage: React.FC<{ 
  params: Promise<{ slug: string }>
 }> = ({  }) => {
    return (
        <div>
            <h1>Blog Page</h1>
            
        </div>
    );
}

export default BlogPage;
